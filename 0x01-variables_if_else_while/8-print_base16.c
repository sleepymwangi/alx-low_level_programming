#include <stdio.h>

/**
 * main - Entries
 * Descriptions: Hexadecimal
 * Return: 0 succession
 */
int main(void)
{
	int a, b;

	for (a = 48; a <= 57; a++)
		putchar(a);
	for (b = 97; b <= 102; b++)
		putchar(b);
	putchar('\n');
	return (0);
}
