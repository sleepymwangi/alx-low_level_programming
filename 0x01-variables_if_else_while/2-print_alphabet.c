#include <stdio.h>

/**
 * main - initialization of character
 * Description: putchar to print alphabet
 * Return:0 succession
 */
int main(void)
{
	char A;

	for (A = 'a'; A <= 'z'; A++)
	{
		putchar(A);
	}
	return (0);
}
